import {useState} from 'react';import useResource from './useResource';import {PlayerButton,Status} from './common';import {rating} from '../lib/format';
export default function Leaderboard({onPlayer,onAwards,refreshKey}){const [period,setPeriod]=useState('all'),[position,setPosition]=useState(''),[clasico,setClasico]=useState(false);const now=new Date(),year=now.getUTCFullYear(),month=now.getUTCMonth()+1;const query=new URLSearchParams();if(period!=='all')query.set('year',period==='month'?year:period);if(period==='month')query.set('month',month);if(position)query.set('position',position);if(clasico)query.set('clasico','true');const {data,error,loading}=useResource(`/stats/leaderboard?${query}`,refreshKey),monthly=useResource(`/stats/awards?year=${year}&month=${month}`,refreshKey),yearly=useResource(`/stats/awards?year=${year}`,refreshKey),history=useResource('/stats/award-history',refreshKey);const rows=data?.leaderboard||[];return <section className="tab-content"><div className="page-title"><p className="eyebrow">GG RANKINGS</p><h2>Leaderboard</h2><p>Performance. Contribution. Results.</p></div><div className="gg-filters"><label>Period<select value={period} onChange={e=>setPeriod(e.target.value)}><option value="all">All Time</option><option value="month">Current Month</option>{[...new Set([year,...(history.data?.years||[])])].map(y=><option key={y} value={y}>{y===year?'Current Season':`${y} Season`}</option>)}</select></label><label>Position<select value={position} onChange={e=>setPosition(e.target.value)}><option value="">Overall</option>{['attackers','midfielders','defenders','goalkeepers'].map(p=><option key={p} value={p}>{p[0].toUpperCase()+p.slice(1)}</option>)}</select></label><label className="gg-check"><input type="checkbox" checked={clasico} onChange={e=>setClasico(e.target.checked)}/> El Clásico only</label></div>{loading||error?<Status loading={loading} error={error}/>:<><div className="gg-podium">{rows.filter(s=>s.ggRating!==null).slice(0,3).map((p,i)=><article key={p.playerId} className={`card gg-place place-${i+1}`}><span>{['🥇','🥈','🥉'][i]} #{i+1}</span><PlayerButton player={p} onPlayer={onPlayer}/><strong className="gg-rating">{rating(p.ggRating)} <small>★</small></strong></article>)}</div>{!rows.some(p=>p.ggRating!==null)&&<Status empty={`No eligible rated players yet. ${clasico?3:5} matches and recorded ratings are needed.`}/>}<div className="gg-table-scroll"><table className="gg-table"><caption className="sr-only">Player rankings</caption><thead><tr>{['Rank','Player','Position','MP','W','D','L','G','A','CS','GG Rating'].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(p=><tr key={p.playerId}><td>{p.rank||'—'}</td><td><PlayerButton player={p} onPlayer={onPlayer}/></td><td>{p.position||p.preferredPositions.join(' / ')||'—'}</td>{['matches','wins','draws','losses','goals','assists','cleanSheets'].map(k=><td key={k}>{p[k]}</td>)}<td><strong>{rating(p.ggRating)}</strong>{p.ggRating===null&&<small>{p.matches<p.minimumMatches?`${p.matches}/${p.minimumMatches} matches`:'Unrated'}</small>}</td></tr>)}</tbody></table></div><div className="gg-two-column">{[['Best Offensive',data?.offensive,'offensiveRating'],['Best Defensive',data?.defensive,'defensiveRating']].map(([title,list,key])=><section className="card" key={key}><h3>{title}</h3>{list?.slice(0,10).map(p=><div className="gg-ranking" key={p.playerId}><PlayerButton player={p} onPlayer={onPlayer}/><span>{key==='offensiveRating'?`${p.goals} G · ${p.assists} A · ${p.goalContributions} GC`:`${p.cleanSheets} CS · ${Math.round(p.cleanSheetRate*100)}% CS · ${p.wins} W`}</span><strong>{rating(p[key])}</strong></div>)}{!list?.length&&<p className="muted">No eligible players yet.</p>}</section>)}</div></>}<div className="gg-two-column">{[['Player of the Month',monthly],['Player of the Year',yearly]].map(([title,resource])=><section className="card" key={title}><p className="eyebrow">CURRENT LEADER · {year} SEASON</p><h3>{title}</h3>{resource.data?.winner?<><PlayerButton player={{...resource.data.winner,playerId:resource.data.winner.player}} onPlayer={onPlayer}/><strong className="gg-rating">{rating(resource.data.winner.value)}</strong></>:<p className="muted">No eligible rated player yet.</p>}<small>Finalized after the period ends.</small></section>)}</div><section className="card gg-awards-entry"><div><p className="eyebrow">AWARDS</p><h3>The GG honours</h3><p className="muted">Monthly recognition and historic seasons.</p></div><button className="secondary-button" onClick={onAwards}>View Awards →</button></section><details className="card"><summary>How GG Rating works</summary><p>40% average recorded match rating + 20% offensive rating + 20% defensive rating + 20% result score. Position does not add points. Votes never affect this rating.</p><p>Offensive and defensive scores use a 0–10 percentile within eligible players in the selected period. Equal performance receives equal scores; a single player or an all-equal cohort scores 5. Legacy appearances remain unrated.</p></details></section>;}
